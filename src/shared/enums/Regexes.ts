export enum Regexes {
    IdOnly,
    EmoteOrMany,
    NameAndId,
    NameAndUrl,
    NameAndEmote,
    HexColor,
    RGBColor,
    DiscordColorResolvableColorName,
    DuplicateIdOnly // We need to add NameAndDuplicateId in the future
}