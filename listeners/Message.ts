import { Message } from "discord.js";
import parseEmotes from "../internal/Parser";
import Emote from "../models/Emote";

export async function run(message: Message, commands: Map<any, any>): Promise<void> {
    
}